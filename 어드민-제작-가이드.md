# 어드민(관리 도구) 제작 가이드 — 강남점

후한의원 홈페이지 관리 도구를 만들거나 고칠 때 따르는 기준입니다.
강남점 관리 도구 v2.0은 전주점 v2.0과 같은 코드이고, 지점 정보만 다릅니다.

---

## 어드민 도구 제작 시 반드시 지켜야 할 것

1. **병합 방식으로 발행한다.**
   글/컬럼 목록을 발행할 때는 기존 목록을 통째로 지우고 새로 채우는 방식(덮어쓰기)이 아니라, 각 글의 파일명(slug)을 기준으로 "있으면 그 항목만 수정, 없으면 새로 추가"하는 방식(병합)으로 구현한다. 목록 파일에서 변경 전 내용을 먼저 읽어온 뒤, 지금 작성 중인 글만 반영하고 나머지 기존 글은 그대로 보존해야 한다.

2. **진짜 원본은 배포된 사이트(저장소)다.**
   글 목록의 "진짜 원본"은 이 도구를 여는 브라우저의 localStorage가 아니라, 실제 배포되어 있는 사이트(또는 저장소)여야 한다. 발행 버튼을 누르면 매번 배포된 목록을 먼저 불러와서 그 위에 병합해야 하고, 브라우저 저장공간에만 있는 값을 기준으로 전체 목록을 다시 만들면 안 된다. 즉, 다른 기기·다른 브라우저·시크릿모드에서 열어도 이전에 올린 글들이 사라지면 안 된다.

3. **새 글 파일명은 배포된 파일과 절대 겹치지 않게 정한다.**
   새 글을 추가할 때 파일명(slug)을 자동으로 만든다면, 이미 존재하는 파일명과 절대 겹치지 않도록 실제 배포된 파일 목록을 확인하고 번호를 매긴다. 단순히 "로컬에 몇 개 있으니 다음 번호"로 정하면 안 된다.

4. **삭제는 사용자가 [삭제]를 눌렀을 때만 일어난다.**
   글을 삭제하는 기능은 사용자가 명확하게 "삭제" 버튼을 눌렀을 때만 실제 삭제로 이어지게 한다. 자동으로 목록이 줄어들거나 빠지는 일이 없어야 한다.

5. **발행 전에 확인 단계를 보여준다.**
   발행하기 전에 "지금 몇 개 글이 목록에 반영될 예정인지, 새로 추가/수정/유지되는 항목이 뭔지" 미리 보여주는 확인 단계를 넣는다.

6. **소스는 GitHub에서 관리하고, Cloudflare Pages는 Git으로 연결한다.**
   이 도구의 소스코드는 GitHub 저장소에 커밋해서 관리하고, Cloudflare Pages는 그 저장소에 연동(Git 연결)해서 배포한다. 파일을 직접 드래그해서 올리는 방식(Direct Upload)은 나중에 수정하거나 버그를 고치기 어려우니 쓰지 않는다.

---

## 이 규칙이 생긴 이유 — v1.0 사고 기록 (2026-09-09 ~ 09-11)

v1.0 관리 도구는 [홈페이지에 바로 반영]을 누르면 그 브라우저에 저장된 글만으로 목록 **전체**와
**모든** 글 파일을 다시 썼고, 새 글 파일명을 `column-(이 브라우저의 글 개수 + 1)`로 정했습니다.
그래서 강남점에서는 다음 글이 "새 컬럼 제목" 빈 글로 덮어써지거나 목록에서 빠졌습니다.

| 사라진 글 | 원래 주소 | 복원한 주소 |
|---|---|---|
| 강남편평사마귀제거 성지 찾는다면? (09-07) | column-04 | column-06 |
| 냉동치료(액화질소) vs 레이저(CO2) 차이와 재발률 (09-09) | column-04 | column-07 |
| 코트라 CO2레이저 vs 일반 CO2레이저 차이 (09-10) | column-05 | column-05 |

git 기록에서 되살렸습니다 (저장소 태그 `v1.0`). 전주점도 같은 일로 5편이 사라졌다가 복원되었습니다.

---

## v2.0은 규칙을 이렇게 지킵니다

| 규칙 | 구현 위치 | 방법 |
|---|---|---|
| 1. 병합 | `columns-core.js` → `planPublish()` | 저장소의 `columns/index.json`을 읽고, 편집 중인 글 1개만 slug 기준으로 바꾸거나 끝에 붙인다. 다른 글의 페이지 파일은 건드리지 않는다. |
| 1. 안전장치 | `planPublish()` 끝부분 | [삭제]로 고른 글 말고 한 개라도 빠지거나, 파일명이 겹치거나, 개수가 안 맞으면 발행을 멈춘다. |
| 2. 원본은 저장소 | `github-publish.js` → `ghSnapshot()` | 발행할 때마다 저장소 최신 커밋을 읽는다. `index.json`이 없거나 깨졌으면 목록을 새로 만들지 않고 멈춘다. 브라우저에는 "발행 전 임시저장본"만 둔다. |
| 2. 동시 발행 | `ghCommit()` | 바뀌는 파일 전부를 커밋 1개로 올리고, 브랜치 갱신은 `force:false`. 그 사이 다른 곳에서 발행했으면 거절되고 최신본으로 다시 계산해 한 번 더 확인받는다. 중간에 실패하면 홈페이지는 그대로다. |
| 3. 파일명 | `collectSlugs()` + `nextSlug()` | 저장소에 실제로 있는 컬럼 파일·원본값 파일·목록의 파일명 중 가장 큰 번호 + 1. 발행 순간에 다시 확인해서, 겹치면 다음 번호로 바꿔 올린다(확인 화면에 표시). |
| 4. 삭제 | `publishDelete()` | [이 글 삭제] → 확인창 → 확인 화면에서 [삭제하고 발행]을 눌러야만 지워진다. |
| 5. 확인 단계 | `showPublishConfirm()` | 발행 후 목록 개수, 추가·수정·유지·삭제 개수와 제목, 바뀌는 파일 목록, 주의사항을 보여준다. |
| 6. Git 배포 | `어드민-설치-순서.md` | 이 소스는 `gnwart` 저장소의 `admin` 브랜치에 있고, Cloudflare Pages가 이 브랜치에 Git으로 연결된다. |

그 밖에:

- **발행 전 검사**: 제목이 비었거나 "새 컬럼 제목"이면, 본문에 "본문을 입력하세요" 문구나 ``` 표시가 남아 있으면 발행을 막는다.
- **다른 곳에서 먼저 수정된 글**: 편집을 시작한 뒤 다른 기기에서 같은 글을 발행했다면, 확인 화면에서 체크해야만 발행된다.
- **글 원본 값 보관**: 글마다 `columns/data/<파일명>.json`에 편집용 원본 값을 둔다. 어느 기기에서 열어도 이어서 고칠 수 있다.
- **페이지에 들어가는 병원 정보**는 브라우저 설정값이 아니라 `columns-core.js`의 `SITE`에서 가져온다.
- **전주점과 섞이지 않게**: 저장소(`gnwart`), GitHub 키 저장 이름(`hoo-gangnam-admin-gh-token`), 브라우저 저장 이름(`hoogangnam_…`)이 모두 전주점과 다르다. 화면 검증은 다른 저장소로 요청이 새지 않는지도 확인한다.

---

## 저장소 구성

| 위치 | 내용 | 배포 |
|---|---|---|
| `gkwngml1215-code/gnwart` → `main` | 강남점 홈페이지 | GitHub Pages → gnhoowart.co.kr |
| `gkwngml1215-code/gnwart` → `admin` | 강남점 관리 도구 (이 폴더) | Cloudflare Pages (Git 연결) |

홈페이지 쪽 컬럼 관련 파일:

```
columns/index.json          ← 컬럼 목록 원본 (발행할 때 이 파일 기준으로 병합)
columns/data/column-XX.json ← 글마다 편집용 원본 값
columns/column-XX.html      ← 실제 글 페이지
columns.html                ← 목록 페이지. COLUMNS:START ~ END 사이만 자동으로 바뀜
sitemap.xml                 ← 컬럼 주소만 자동으로 바뀜
llms.txt                    ← "## 원장 컬럼" 구역의 컬럼 줄만 자동으로 바뀜
```

관리 도구 파일:

```
index.html          ← 화면 (1~7번 SEO 설정은 강남점 값, 검색엔진 소유확인 입력칸 포함)
columns-core.js     ← 병합·파일명·페이지 생성 + 강남점 설정(SITE)
github-publish.js   ← GitHub 읽기·쓰기, 발행 전 확인 화면
_worker.js          ← 아이디·비밀번호 잠금 (지우면 잠금이 풀림)
tests/              ← 검증 스크립트
```

---

## 고친 뒤에는 반드시 검증

```bash
# 1) 규칙 검증 (병합·파일명·삭제·안전장치)
node tests/columns-core.test.js

# 2) 실제 화면 검증 — 가짜 저장소로 버튼을 직접 눌러 봄 (실제 홈페이지는 안 바뀜)
node tests/e2e-admin.js "<강남점 홈페이지 저장소 폴더>" "C:/Program Files/Google/Chrome/Application/chrome.exe" "<임시 폴더>"
```

---

## 전주점과 다른 점 (`columns-core.js`의 `SITE`)

| 항목 | 강남점 | 전주점 |
|---|---|---|
| 도메인 | gnhoowart.co.kr | jjhoowart.co.kr |
| 저장소 | gnwart | jjwart |
| 대표원장 / 전화 | 홍진우 / 02-543-9696 | 허정위 / 063-251-1050 |
| 카카오 | pf.kakao.com/_LYNVu | pf.kakao.com/_triUj |
| 기본 사진 | doctor-hong.jpg (위쪽 기준 자르기 `object-top`) | doctor-heo.jpg |
| 푸터 | … CLINIC GANGNAM | … CLINIC JEONJU |

---

## 버전 기록

| 버전 | 태그 | 내용 |
|---|---|---|
| v1.0 | 홈페이지 `v1.0`, 관리 도구 `admin-v1.0` | 브라우저 저장값 기준 덮어쓰기 방식 (위 사고의 원인). 홈페이지는 사라진 컬럼 3편을 복원한 상태(7편). |
| v2.0 | 홈페이지 `v2.0`, 관리 도구 `admin-v2.0` | 저장소 원본(`columns/index.json`) 기준 병합 발행, 발행 전 확인 화면, 파일명 충돌 방지, 명시적 삭제, 커밋 1개로 발행, 동시 발행 보호, Cloudflare Git 연결. |
