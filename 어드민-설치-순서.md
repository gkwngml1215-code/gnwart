# 후한의원 강남점 어드민 설치 순서 (한 번만 하면 됩니다)

아이디·비밀번호로 잠긴 **강남점 전용** 어드민을 인터넷에 올리는 절차입니다.
전주점 어드민(`hoo-admin.pages.dev`)과는 **완전히 별개**로 새로 만듭니다.

전부 무료입니다. 카드 등록도 필요 없습니다.

---

## ⚠️ 시작 전에 — 전주점과 무엇이 다른가

| | 전주점 (기존) | 강남점 (지금 만들 것) |
|---|---|---|
| Cloudflare 프로젝트 | `hoo-admin` | **`hoo-gangnam-admin`** |
| 어드민 주소 | hoo-admin.pages.dev | **hoo-gangnam-admin.pages.dev** |
| 글이 올라가는 저장소 | `jjwart` | **`gnwart`** |
| 홈페이지 주소 | jjhoowart.co.kr | **gnhoowart.co.kr** (임시) |

이 폴더의 어드민은 `gnwart` 저장소로만 글을 보냅니다.
**전주점 홈페이지에는 어떤 경우에도 영향을 주지 않습니다.**

---

## 준비물

- 이 폴더 (`hoo-gangnam-admin`) — 안에 4개가 들어있습니다
  - `_worker.js`  ← **아이디·비밀번호를 검사하는 파일**
  - `index.html`  ← 어드민 화면
  - `github-publish.js`
  - `어드민-설치-순서.md` (이 문서)
- GitHub 계정 (이미 있으심)

> **주의:** `_worker.js` 는 절대 지우거나 이름을 바꾸지 마세요.
> 이 파일이 없으면 **잠금이 통째로 풀려** 누구나 어드민에 들어옵니다.
> 파일 이름 앞의 밑줄(`_`)까지 정확해야 합니다.

---

## 0단계 — GitHub에 강남점 저장소 만들기

어드민보다 **이것을 먼저** 만들어야 합니다.

1. https://github.com/new 접속
2. Repository name 에 `gnwart` 입력
3. **Public** 선택 (GitHub Pages 무료로 쓰려면 Public 이어야 합니다)
4. **[Create repository]** 클릭
5. `hoo-gangnam-landing` 폴더를 이 저장소에 올립니다 (아래 명령을 폴더에서 한 번만)

```
git init
git add -A
git commit -m "후한의원 강남점 랜딩페이지 초기 구축"
git branch -M main
git remote add origin https://github.com/gkwngml1215-code/gnwart.git
git push -u origin main
```

6. 저장소 **Settings → Pages** → Source 를 **Deploy from a branch**,
   Branch 를 **main / (root)** 로 지정하고 저장

> 이 다음부터는 `hoo-gangnam-landing` 폴더의 **「사이트에-올리기.bat」** 만 눌러도 됩니다.
> 그 파일에는 실수로 전주점 저장소에 올라가지 않도록 안전장치가 들어 있습니다.

---

## 1단계 — GitHub 접근 키 발급

어드민이 사장님 대신 홈페이지에 글을 올리려면 열쇠가 필요합니다.

> 전주점 어드민에 쓰던 키는 `jjwart` 전용으로 발급돼 있어서 **강남점에는 쓸 수 없습니다.**
> 새로 하나 더 발급받으세요. (또는 기존 키의 Repository access 에 `gnwart` 를 추가해도 됩니다)

1. https://github.com/settings/personal-access-tokens 접속
2. 오른쪽 위 **[Generate new token]** 클릭
3. 아래처럼 입력합니다

   | 항목 | 넣을 값 |
   |---|---|
   | Token name | `후한의원 강남점 어드민` |
   | Expiration | **No expiration** (만료 없음) |
   | Repository access | **Only select repositories** → **`gnwart`** 선택 |

4. **Permissions** → **Repository permissions** 를 펼칩니다
5. 목록에서 **Contents** 를 찾아 **Read and write** 로 바꿉니다

   > 이거 하나만 바꾸면 됩니다. 나머지는 손대지 마세요.

6. 맨 아래 **[Generate token]** 클릭
7. `github_pat_` 로 시작하는 긴 문자열이 나옵니다.
   **이 화면을 벗어나면 다시 볼 수 없습니다.** 메모장에 복사해 두세요.

---

## 2단계 — Cloudflare에 어드민 올리기

1. https://dash.cloudflare.com 로그인 (전주점 때 만든 계정 그대로 쓰시면 됩니다)
2. 왼쪽 메뉴에서 **Workers & Pages** 클릭
3. **[Create]** → 위쪽 탭에서 **Pages** 선택 → **[Upload assets]** 클릭
4. 프로젝트 이름에 **`hoo-gangnam-admin`** 을 입력합니다

   > ⚠️ 기존 `hoo-admin` 을 고르면 **전주점 어드민이 강남점으로 덮어써집니다.**
   > 반드시 **새 프로젝트**를 만드세요.

5. **[Create project]** 클릭
6. **`hoo-gangnam-admin` 폴더를 통째로 끌어다 놓으세요.**
7. 올리기 전에 목록에 **`_worker.js` 가 보이는지 반드시 확인**하세요.
8. **[Deploy site]** 클릭

이제 주소가 생겼습니다. **하지만 아직 비밀번호가 없어서 누구나 들어갈 수 있습니다.**
바로 3단계로 가세요.

---

## 3단계 — 아이디·비밀번호 정하기

1. 방금 만든 `hoo-gangnam-admin` 프로젝트에서 **Settings** 탭 클릭
2. **Variables and Secrets** (환경 변수) 항목을 찾습니다
3. **[Add]** 를 눌러 **2개**를 추가합니다

   | 이름 (Variable name) | 값 (Value) | 종류 |
   |---|---|---|
   | `ADMIN_USER` | 원하는 아이디 (예: `GNWART`) | Secret |
   | `ADMIN_PASS` | 원하는 비밀번호 | Secret |

   > - 이름은 **대문자로 정확히** 위와 같이 적어야 합니다.
   > - 종류는 **Secret** 을 고르세요.
   > - 적용 대상은 **Production** 을 선택합니다.
   > - 전주점과 **다른 비밀번호**를 쓰시는 편이 안전합니다.

4. **[Save]** 클릭
5. **Deployments** 탭 → 최신 배포 오른쪽 **[...]** → **[Retry deployment]** 클릭

   > 환경 변수는 다시 배포해야 적용됩니다. 이 단계를 빼먹으면 계속 오류가 납니다.

---

## 4단계 — 잠금 확인

1. 시크릿 창(Ctrl+Shift+N)으로 `https://hoo-gangnam-admin.pages.dev` 접속
2. **아이디·비밀번호 입력창이 뜨면 성공입니다**

### 이런 화면이 나온다면

| 화면 | 원인 | 해결 |
|---|---|---|
| 입력창 없이 어드민이 바로 열림 | `_worker.js` 가 안 올라감 | 2단계를 다시 하되 `_worker.js` 포함 확인 |
| `설정 오류: ... ADMIN_USER ...` | 환경 변수 이름이 틀림 | 3단계 표와 글자를 하나씩 대조 |
| 맞는 비번인데 계속 물어봄 | 재배포를 안 함 | 3단계 5번(Retry deployment) 실행 |

---

## 5단계 — 어드민에 GitHub 키 넣기

1. 어드민에 로그인한 상태에서 화면 아래쪽 **[🔑 GitHub 연결]** 클릭
2. 1단계에서 복사해 둔 `github_pat_...` 문자열을 붙여넣고 확인
3. **"GitHub 연결됨: gkwngml1215-code/gnwart"** 라고 뜨면 끝입니다

   > 여기에 **`gnwart`** 라고 나오는지 꼭 확인하세요.
   > `jjwart` 로 나오면 잘못된 파일이 올라간 것입니다.

이 키는 그 기기의 브라우저에만 저장됩니다.
**새 기기(폰, 다른 PC)에서 처음 쓸 때는 이 단계를 한 번 더 해야 합니다.**

---

---

## 어드민 화면이 바뀌었을 때 — 다시 올리는 법

어드민 파일(`index.html`, `github-publish.js`)이 수정되면,
Cloudflare에 **다시 올려야** 인터넷 주소에서도 바뀐 화면이 보입니다.

1. https://dash.cloudflare.com 접속 → **Workers & Pages** → **hoo-gangnam-admin** 클릭
2. **Deployments** 탭 → **[Create deployment]** (또는 **[Upload assets]**) 클릭
3. **`hoo-gangnam-admin` 폴더를 통째로** 끌어다 놓습니다
4. 목록에 **`_worker.js` 가 보이는지 확인** (없으면 잠금이 풀립니다)
5. **[Deploy site]** 클릭 → 1분 뒤 `https://hoo-gangnam-admin.pages.dev` 새로고침

> - 아이디·비밀번호는 프로젝트에 저장되어 있으므로 **다시 정할 필요 없습니다.**
> - 작성해 둔 컬럼 내용도 **그대로 남아 있습니다.** (브라우저에 저장되어 있고, 주소가 같기 때문)
> - 화면이 그대로면 **Ctrl+Shift+R** 로 새로고침해 주세요.

### 주의 — 컴퓨터에 있는 파일을 직접 열지 마세요

`index.html` 을 더블클릭해서 여는 것과 `hoo-gangnam-admin.pages.dev` 에 접속하는 것은
**서로 다른 저장 공간**을 씁니다.
파일을 직접 열면 **그동안 쓴 컬럼이 하나도 안 보이고**,
그 상태로 [홈페이지에 바로 반영]을 누르면 **기존 컬럼 목록이 지워집니다.**

반드시 `https://hoo-gangnam-admin.pages.dev` 로 접속해서 쓰세요.

---

## 다 끝났습니다 — 이제 이렇게 씁니다

1. 어디서든 `https://hoo-gangnam-admin.pages.dev` 접속
2. 아이디·비밀번호 입력
3. 컬럼 작성
4. **[홈페이지에 바로 반영]** 클릭
5. 1~2분 뒤 강남점 홈페이지에 올라갑니다

---

## 알아두실 것

**전주점 어드민과 헷갈리지 않도록 즐겨찾기 이름을 다르게 해두세요.**
(예: "후한 전주 어드민" / "후한 강남 어드민")

**두 어드민은 저장 공간이 분리되어 있습니다.** 강남점 어드민에서 쓴 글이
전주점 어드민 목록에 나타나거나 그 반대가 되는 일은 없습니다.

**비밀번호를 바꾸려면** 3단계의 `ADMIN_PASS` 값을 고치고 다시 배포하면 됩니다.

**폰이나 노트북을 잃어버렸다면** GitHub에서 그 키를 폐기하세요.
https://github.com/settings/personal-access-tokens → 해당 토큰 → Delete

**어드민 화면이 예전 모습으로 보이면** Ctrl+F5 로 강력 새로고침 하세요.
